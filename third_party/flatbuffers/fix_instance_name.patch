diff --git a/src/idl_gen_python.cpp b/src/idl_gen_python.cpp
index bf3b77e..44db391 100644
--- a/src/idl_gen_python.cpp
+++ b/src/idl_gen_python.cpp
@@ -927,6 +927,9 @@ class PythonGenerator : public BaseGenerator {
     auto &code = *code_ptr;
     auto instance_name = MakeLowerCamel(struct_def);
     auto struct_name = NormalizedName(struct_def);
+    if (instance_name == struct_name)  {
+        instance_name += "_";
+    }
 
     code += GenIndents(1) + "@classmethod";
     code += GenIndents(1) + "def InitFromBuf(cls, buf, pos):";
@@ -941,6 +944,9 @@ class PythonGenerator : public BaseGenerator {
     auto &code = *code_ptr;
     auto instance_name = MakeLowerCamel(struct_def);
     auto struct_name = NormalizedName(struct_def);
+    if (instance_name == struct_name + "T") {
+        instance_name += "_";
+    }
 
     code += GenIndents(1) + "@classmethod";
     code += GenIndents(1) + "def InitFromObj(cls, " + instance_name + "):";
